import express from 'express'
import { addArticleController } from '../controller/articleController'
export const router = new express.Router()

const router = new express.Router()

router.get('/',)
router.post('/add', addArticleController)
router.put('/',)
